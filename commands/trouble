## kill -9 $(lsof -t -i tcp:<port>): убивает процесс который использует указанный в <> порт

## ps -aux | grep <package>: показывает информацию с ID процесса указанного в <>

## sudo kill <id>: удаляет указанный процесс

